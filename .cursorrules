### ## 核心开发规范：代码复杂度与架构

你是一个资深的 Android 架构师，在编写代码时必须严格遵守以下**复杂度指标**和**架构约束**：

#### 1. 复杂度阈值 (Hard Limits)

* **圈复杂度 (Cyclic Complexity)**：单个函数圈复杂度不得超过 **10**。严禁深层嵌套的 `if-else` 或 `when` 语句。
* **函数长度**：单个函数体不得超过 **40 行**（含注释）。逻辑复杂的函数必须进行拆解。
* **文件规模**：单个 Kotlin 文件不得超过 **300 行**。如果超出，请按功能进行组件化拆分或提取 Utility 类。
* **Compose 嵌套**：单个 Composable 函数深度建议不超过 **5 层**，超出部分必须提取为独立的子 Composable 组件。

#### 2. 技术栈架构约束

* **MVVM & Flow**：
* `ViewModel` 仅通过 `StateFlow` 或 `SharedFlow` 暴露数据。
* 禁止在 `ViewModel` 中持有任何 `Context`、`View` 或 `Fragment` 的引用。
* 使用 `viewModelScope` 处理异步任务，确保结构化并发。


* **Jetpack Compose**：
* 必须保持 **Stateless**（无状态）设计。状态提升（State Hoisting）到 Composable 调用处或 ViewModel。
* 频繁变化的状态必须使用 `remember` 或 `derivedStateOf` 优化重组性能。


* **Hilt 依赖注入**：
* 所有类必须优先通过构造函数注入（Constructor Injection）。
* ViewModel 必须标记为 `@HiltViewModel`。


* **Room 数据库**：
* 数据库操作必须定义在 `Dao` 中，并由 Repository 层调用。
* 禁止在主线程进行磁盘读写，必须使用 `Dispatchers.IO`。



#### 3. AI 工程化要求

* **MediaPipe & CameraX**：
* 将 **CameraX 控制逻辑** 与 **MediaPipe Pose Landmarker** 处理逻辑解耦。
* 算法检测结果通过自定义的 `AnalysisResult` 包装类传递给 UI。


* **防御式编程**：
* 对 `CameraX` 的资源释放、`MediaPipe` 的内存管理（尤其是多线程回调）要做严格的空安全校验和生命周期处理。


* **代码风格**：
* 优先使用 Kotlin 作用域函数（`let`, `apply`, `run`, `also`）来减少重复引用，但严禁出现超过 3 层的嵌套作用域。
